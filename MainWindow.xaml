<Window x:Class="PotatoBooster.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Potato Booster v1.0" Height="620" Width="480"
        WindowStartupLocation="CenterScreen"
        Background="#181818" Foreground="#D4D4D4"
        ResizeMode="CanMinimize" Closing="Window_Closing"
        Opacity="0">

    <!-- Window Fade-In Animation -->
    <Window.Triggers>
        <EventTrigger RoutedEvent="Window.Loaded">
            <BeginStoryboard>
                <Storyboard>
                    <DoubleAnimation Storyboard.TargetProperty="Opacity" From="0" To="1" Duration="0:0:0.4"/>
                </Storyboard>
            </BeginStoryboard>
        </EventTrigger>
    </Window.Triggers>

    <Window.Resources>
        <!-- Standard Animated Button -->
        <Style TargetType="Button">
            <Setter Property="Background" Value="#2A2A2C"/>
            <Setter Property="Foreground" Value="#CCCCCC"/>
            <Setter Property="BorderBrush" Value="#45454E"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Height" Value="28"/>
            <Setter Property="Margin" Value="0,0,0,6"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="RenderTransformOrigin" Value="0.5,0.5"/>
            <Setter Property="RenderTransform">
                <Setter.Value>
                    <ScaleTransform ScaleX="1" ScaleY="1"/>
                </Setter.Value>
            </Setter>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <EventTrigger RoutedEvent="MouseEnter">
                                <BeginStoryboard>
                                    <Storyboard>
                                        <DoubleAnimation Storyboard.TargetProperty="(RenderTransform).(ScaleTransform.ScaleX)" To="1.02" Duration="0:0:0.1"/>
                                        <DoubleAnimation Storyboard.TargetProperty="(RenderTransform).(ScaleTransform.ScaleY)" To="1.02" Duration="0:0:0.1"/>
                                        <ColorAnimation Storyboard.TargetName="border" Storyboard.TargetProperty="(Border.Background).(SolidColorBrush.Color)" To="#38383E" Duration="0:0:0.1"/>
                                    </Storyboard>
                                </BeginStoryboard>
                            </EventTrigger>
                            <EventTrigger RoutedEvent="MouseLeave">
                                <BeginStoryboard>
                                    <Storyboard>
                                        <DoubleAnimation Storyboard.TargetProperty="(RenderTransform).(ScaleTransform.ScaleX)" To="1.0" Duration="0:0:0.1"/>
                                        <DoubleAnimation Storyboard.TargetProperty="(RenderTransform).(ScaleTransform.ScaleY)" To="1.0" Duration="0:0:0.1"/>
                                        <ColorAnimation Storyboard.TargetName="border" Storyboard.TargetProperty="(Border.Background).(SolidColorBrush.Color)" To="#2A2A2C" Duration="0:0:0.1"/>
                                    </Storyboard>
                                </BeginStoryboard>
                            </EventTrigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Big Apply Accent Button with Click Pulse -->
        <Style x:Key="ApplyButton" TargetType="Button">
            <Setter Property="Background" Value="#0E639C"/>
            <Setter Property="Foreground" Value="#FFFFFF"/>
            <Setter Property="BorderBrush" Value="#1177BB"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Height" Value="36"/>
            <Setter Property="Margin" Value="0,6,0,6"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="RenderTransformOrigin" Value="0.5,0.5"/>
            <Setter Property="RenderTransform">
                <Setter.Value>
                    <ScaleTransform ScaleX="1" ScaleY="1"/>
                </Setter.Value>
            </Setter>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <EventTrigger RoutedEvent="MouseEnter">
                                <BeginStoryboard>
                                    <Storyboard>
                                        <DoubleAnimation Storyboard.TargetProperty="(RenderTransform).(ScaleTransform.ScaleX)" To="1.03" Duration="0:0:0.1"/>
                                        <DoubleAnimation Storyboard.TargetProperty="(RenderTransform).(ScaleTransform.ScaleY)" To="1.03" Duration="0:0:0.1"/>
                                        <ColorAnimation Storyboard.TargetName="border" Storyboard.TargetProperty="(Border.Background).(SolidColorBrush.Color)" To="#1177BB" Duration="0:0:0.1"/>
                                    </Storyboard>
                                </BeginStoryboard>
                            </EventTrigger>
                            <EventTrigger RoutedEvent="MouseLeave">
                                <BeginStoryboard>
                                    <Storyboard>
                                        <DoubleAnimation Storyboard.TargetProperty="(RenderTransform).(ScaleTransform.ScaleX)" To="1.0" Duration="0:0:0.1"/>
                                        <DoubleAnimation Storyboard.TargetProperty="(RenderTransform).(ScaleTransform.ScaleY)" To="1.0" Duration="0:0:0.1"/>
                                        <ColorAnimation Storyboard.TargetName="border" Storyboard.TargetProperty="(Border.Background).(SolidColorBrush.Color)" To="#0E639C" Duration="0:0:0.1"/>
                                    </Storyboard>
                                </BeginStoryboard>
                            </EventTrigger>
                            <EventTrigger RoutedEvent="PreviewMouseDown">
                                <BeginStoryboard>
                                    <Storyboard>
                                        <DoubleAnimation Storyboard.TargetProperty="(RenderTransform).(ScaleTransform.ScaleX)" To="0.96" Duration="0:0:0.05"/>
                                        <DoubleAnimation Storyboard.TargetProperty="(RenderTransform).(ScaleTransform.ScaleY)" To="0.96" Duration="0:0:0.05"/>
                                    </Storyboard>
                                </BeginStoryboard>
                            </EventTrigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Animated CheckBox -->
        <Style TargetType="CheckBox">
            <Setter Property="Foreground" Value="#D4D4D4"/>
            <Setter Property="Margin" Value="0,0,0,8"/>
            <Setter Property="RenderTransformOrigin" Value="0,0.5"/>
            <Setter Property="RenderTransform">
                <Setter.Value>
                    <ScaleTransform ScaleX="1" ScaleY="1"/>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <EventTrigger RoutedEvent="MouseEnter">
                    <BeginStoryboard>
                        <Storyboard>
                            <DoubleAnimation Storyboard.TargetProperty="(RenderTransform).(ScaleTransform.ScaleX)" To="1.02" Duration="0:0:0.1"/>
                            <DoubleAnimation Storyboard.TargetProperty="(RenderTransform).(ScaleTransform.ScaleY)" To="1.02" Duration="0:0:0.1"/>
                        </Storyboard>
                    </BeginStoryboard>
                </EventTrigger>
                <EventTrigger RoutedEvent="MouseLeave">
                    <BeginStoryboard>
                        <Storyboard>
                            <DoubleAnimation Storyboard.TargetProperty="(RenderTransform).(ScaleTransform.ScaleX)" To="1.0" Duration="0:0:0.1"/>
                            <DoubleAnimation Storyboard.TargetProperty="(RenderTransform).(ScaleTransform.ScaleY)" To="1.0" Duration="0:0:0.1"/>
                        </Storyboard>
                    </BeginStoryboard>
                </EventTrigger>
            </Style.Triggers>
        </Style>

        <Style TargetType="TabItem">
            <Setter Property="Background" Value="#202020"/>
            <Setter Property="Foreground" Value="#A0A0A0"/>
            <Setter Property="BorderBrush" Value="#3F3F46"/>
            <Setter Property="Padding" Value="12,6"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TabItem">
                        <Border x:Name="Border" Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="1,1,1,0" Margin="0,0,2,0">
                            <ContentPresenter x:Name="ContentSite" VerticalAlignment="Center" HorizontalAlignment="Center" 
                                              ContentSource="Header" Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="#181818"/>
                                <Setter Property="Foreground" Value="#007ACC"/>
                                <Setter Property="FontWeight" Value="Bold"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Background="#202020" BorderBrush="#3F3F46" BorderThickness="1" Padding="8,6" Margin="0,0,0,10">
            <DockPanel>
                <TextBlock Text="POTATO BOOSTER" FontSize="13" FontWeight="Bold" Foreground="#007ACC" VerticalAlignment="Center"/>
                <TextBlock Text="v1.0" FontSize="11" Foreground="#777777" VerticalAlignment="Center" HorizontalAlignment="Right"/>
            </DockPanel>
        </Border>

        <!-- Main Body -->
        <TabControl Grid.Row="1" Background="#181818" BorderBrush="#3F3F46" BorderThickness="1">
            <TabItem Header="Booster">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <StackPanel Margin="10">
                        <Border Background="#202020" BorderBrush="#3F3F46" BorderThickness="1" Padding="10" Margin="0,0,0,10">
                            <StackPanel>
                                <TextBlock Text="Optimizations:" FontSize="11" FontWeight="Bold" Foreground="#007ACC" Margin="0,0,0,8"/>
                                <CheckBox x:Name="ChkCleanTemp" Content="Clean Temp &amp; Caches (Recommended)" IsChecked="True"/>
                                <CheckBox x:Name="ChkPowerPlan" Content="Ultimate Power Plan (Recommended)" IsChecked="True"/>
                                <CheckBox x:Name="ChkFlushRam" Content="Flush System RAM (Recommended)" IsChecked="True"/>
                                <CheckBox x:Name="ChkNetwork" Content="Optimize Network &amp; Flush DNS" IsChecked="False"/>
                                <CheckBox x:Name="ChkCpuUnpark" Content="Unpark CPU Cores" IsChecked="False"/>
                                <CheckBox x:Name="ChkTelemetry" Content="Disable Tracking &amp; Telemetry" IsChecked="False"/>
                                <CheckBox x:Name="ChkWinsock" Content="Reset Winsock &amp; IP Stack" IsChecked="False"/>
                                <CheckBox x:Name="ChkEventLogs" Content="Clear Windows Event Logs" IsChecked="False"/>
                            </StackPanel>
                        </Border>

                        <Border Background="#202020" BorderBrush="#3F3F46" BorderThickness="1" Padding="8" Margin="0,0,0,10">
                            <StackPanel>
                                <TextBlock Text="Target Game Process:" FontSize="11" Foreground="#A0A0A0" Margin="0,0,0,4"/>
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="65"/>
                                        <ColumnDefinition Width="80"/>
                                    </Grid.ColumnDefinitions>

                                    <ComboBox x:Name="CboProcesses" Grid.Column="0" Height="26" Background="#2D2D30" 
                                              Foreground="#000000" BorderBrush="#3F3F46" BorderThickness="1" VerticalContentAlignment="Center"/>
                                    <Button Grid.Column="1" Content="Scan" Margin="4,0,0,0" Height="26" Click="BtnRefreshProcesses_Click"/>
                                    <Button Grid.Column="2" Content="Boost" Margin="4,0,0,0" Height="26" Click="BtnBoostGame_Click"/>
                                </Grid>
                            </StackPanel>
                        </Border>

                        <Button Content="APPLY" Style="{StaticResource ApplyButton}" Click="BtnBoostAll_Click"/>
                    </StackPanel>
                </ScrollViewer>
            </TabItem>

            <TabItem Header="Settings">
                <StackPanel Margin="10">
                    <TextBlock Text="Options" FontSize="11" FontWeight="Bold" Foreground="#007ACC" Margin="0,0,0,10"/>

                    <CheckBox x:Name="ChkGpuCache" Content="Include GPU Shader Cache" IsChecked="True"/>
                    <CheckBox x:Name="ChkAggressiveRam" Content="Aggressive RAM Flush"/>
                    <CheckBox x:Name="ChkDisableVisualEffects" Content="Disable Windows Animations"/>

                    <Border BorderBrush="#3F3F46" BorderThickness="0,1,0,0" Margin="0,5,0,10"/>

                    <TextBlock Text="Custom Game Process Names:" FontSize="11" Foreground="#A0A0A0" Margin="0,0,0,4"/>
                    <TextBox x:Name="TxtCustomGames" Height="26" Background="#252526" Foreground="#FFFFFF" 
                             BorderBrush="#3F3F46" BorderThickness="1" Padding="4,2" FontSize="11"
                             Text="mygame, customapp"/>

                    <Button Content="Save Configuration" Margin="0,15,0,0" Click="BtnSaveConfig_Click"/>
                </StackPanel>
            </TabItem>
        </TabControl>

        <!-- Animated Log Terminal -->
        <StackPanel Grid.Row="2" Margin="0,8,0,0">
            <TextBlock Text="Log:" FontSize="11" Foreground="#888888" Margin="0,0,0,4"/>
            <Border Background="#101010" BorderBrush="#3F3F46" BorderThickness="1" Height="110" Padding="6">
                <ScrollViewer x:Name="LogScrollViewer" VerticalScrollBarVisibility="Auto">
                    <StackPanel x:Name="LogContainer"/>
                </ScrollViewer>
            </Border>
        </StackPanel>
    </Grid>
</Window>